name: elbo-comparison
channels:
  - defaults
dependencies:
  - tqdm
  - jupyter
  - notebook
  - matplotlib
  - pytorch
  - torchvision
  - cudatoolkit=10.2
prefix: /home/arend/miniconda3/envs/elbo-comparison
